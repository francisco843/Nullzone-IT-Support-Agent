{
  "name": "remote-panel-agent",
  "version": "2.0.0",
  "description": "Local agent for Remote Shell Panel — exposes a full PTY shell via outbound WebSocket",
  "main": "agent.js",
  "scripts": {
    "start": "node agent.js",
    "dev":   "node agent.js"
  },
  "dependencies": {
    "ws":       "^8.18.0",
    "dotenv":   "^16.5.0",
    "node-pty": "^1.0.0"
  },
  "engines": {
    "node": ">=18 <25"
  }
}
